import matplotlib.pyplot as mpl
# RandomForestClassifier から AdaBoostClassifier に変更
from sklearn.ensemble import AdaBoostClassifier as ABC

from sklearn.datasets import fetch_openml
mnist = fetch_openml('mnist_784', as_frame=False)

x = mnist.data.reshape (70000 , 784) / 255
y = mnist.target.astype(int)

# AdaBoostモデルを初期化（乱数シード 0）
model = ABC(random_state =0)
model.fit(x, y)
scr = model.score(x, y)

yres = model.predict(x)

print(" Prediction accuracy =", scr)
print(yres)

from sklearn.metrics import classification_report
print(classification_report(y,yres))

from sklearn.metrics import confusion_matrix
cm = confusion_matrix(y,yres)
print(cm)

import seaborn as sns
sns.heatmap(cm, annot=True , cmap='Blues')
mpl.show()
